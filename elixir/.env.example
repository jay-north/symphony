# Copy this file to `.env` and fill in local values.
# `.env` is ignored by git and loaded automatically by `./bin/symphony`.

LINEAR_API_KEY=

# Optional: route Symphony to a specific Linear user.
# LINEAR_ASSIGNEE=me

# Optional: override paths used by WORKFLOW.md.
# SYMPHONY_WORKSPACE_ROOT=~/code/symphony-workspaces
